<Window x:Class="Lab_rab_4_2_CherevkoG.S_BPI_23_02.View.WindowNewEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:Lab_rab_4_2_CherevkoG.S_BPI_23_02.View"
        Title="Новый сотрудник" Height="300" Width="300"
        WindowStartupLocation="CenterOwner"
        Style="{DynamicResource CustomWindowStyle}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Код:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Id}" IsReadOnly="True" Margin="5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Должность:"/>
        <ComboBox x:Name="cbRole" Grid.Row="1" Grid.Column="1" 
                  DisplayMemberPath="NameRole" SelectedValuePath="Id"
                  Margin="5" Height="25"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Фамилия:"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding LastName}" Margin="5"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Имя:"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding FirstName}" Margin="5"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Дата рождения:"/>
        <Grid Grid.Row="4" Grid.Column="1" Margin="5">
            <DatePicker x:Name="ClBirthday" Visibility="Hidden"
                SelectedDate="{Binding Birthday, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                VerticalAlignment="Center"/>

            <TextBox x:Name="tbBirthday" 
             Text="{Binding Birthday, UpdateSourceTrigger=PropertyChanged}"
             IsVisibleChanged="tbBirthday_IsVisibleChanged"
             VerticalAlignment="Center" 
             Background="Black"/>
        </Grid>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="80" Margin="5"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>