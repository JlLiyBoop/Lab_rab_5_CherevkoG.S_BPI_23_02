<Window x:Class="Lab_rab_4_2_CherevkoG.S_BPI_23_02.View.WindowEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_rab_4_2_CherevkoG.S_BPI_23_02.View"
        mc:Ignorable="d"
        Title="Сотрудники" Height="450" Width="800"
        Style="{DynamicResource CustomWindowStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="Добавить" Height="25" Margin="10,10,5,10"
                    Command="{Binding AddPerson}"/>
            <Button Content="Редактировать" Height="25" Margin="10,10,5,10"
                    Command="{Binding EditPerson}"/>
            <Button Content="Удалить" Height="25" Margin="10,10,5,10"
                    Command="{Binding DeletePerson}"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding ListPersonDpo}"
                  SelectedItem="{Binding SelectedPersonDpo}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Код" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Должность" DisplayMemberBinding="{Binding RoleName}" Width="150"/>
                    <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding LastName}" Width="150"/>
                    <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding FirstName}" Width="150"/>
                    <GridViewColumn Header="Дата рождения" DisplayMemberBinding="{Binding Birthday, StringFormat=dd.MM.yyyy}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>